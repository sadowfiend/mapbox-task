/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    images: {
        unoptimized: true,
    },
    // basePath: '/mapbox-task',
    // assetPrefix: '/mapbox-task/',
};

export default nextConfig;
